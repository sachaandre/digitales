<p id="notice"><%= notice %></p>

<h1><%= @country.name %></h1>

<table>
    <tbody>
        <% @country.aventures.each do |aventure| %>
            <tr>
                <td><%= link_to aventure.title, aventure %></td>
            </tr>
        <% end %>
    </tbody>
</table>
